<!DOCTYPE html>
<html>
	<head>
		<meta charset="utf-8">
		<meta name="viewport" content="initial-scale=1">
		<link rel="stylesheet" href="{{reversepath}}../styles/hella.css">
		<link rel="stylesheet" href="{{reversepath}}../styles/style.css">
		<link rel="stylesheet" href="{{reversepath}}../styles/hella-style.css">
		<title>badnoise pg {{title}}</title>
	</head>
	<body>
		<a href="index.html">
      <div id="header">
	       <div id="logo"></div>
			</div>
		</a>
    <div class="grid">
      <div class="row" id="navbar">
        <div class="column six non-responsive no-margin">
					<a href="#">blog</a>
				</div>
				<div class="column six non-responsive">
					<a href="#">contact</a>
				</div>
      </div>
			<div class="content">
				<div class="row ">
					<div class="column">
						<h3>{{title}}</h3>
						{{content}}
					</div>
				</div>
      </div>
    </div>
		<div id="bottombar"></div>
	</body>
</html>
